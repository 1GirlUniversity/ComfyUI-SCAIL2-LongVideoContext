# Development notes

Test locally by cloning or symlinking this folder into `ComfyUI/custom_nodes/`, then restart ComfyUI and search for:

- SCAIL-2 Smart Long Video Context
- SCAIL-2 Long Video Context

Before publishing, edit `pyproject.toml`:

- Replace `YOUR_GITHUB_USERNAME`.
- Replace `YOUR_COMFY_REGISTRY_PUBLISHER_ID`.
- Update `version` for each release.
